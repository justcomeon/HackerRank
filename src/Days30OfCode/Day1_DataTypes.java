package Days30OfCode;
/*
 * https://www.hackerrank.com/challenges/30-data-types/submissions/code/39534636
 */

public class Day1_DataTypes {
    /* Declare second integer, double, and String variables. */

    /* Read and save an integer, double, and String to your variables.*/
    // Note: If you have trouble reading the entire String, please go back and review the Tutorial closely.

    /* Print the sum of both integer variables on a new line. */

    /* Print the sum of the double variables on a new line. */
	
    /* Concatenate and print the String variables on a new line; 
    	the 's' variable above should be printed first. */
	/*
	int i_input;
    double d_input;
    String s_input;

    i_input = scan.nextInt();
    d_input = scan.nextDouble();
    scan.nextLine();
    s_input = scan.nextLine();

    System.out.println(i+i_input);
    System.out.println(d+d_input);
    System.out.println(s+s_input);
    */
}
